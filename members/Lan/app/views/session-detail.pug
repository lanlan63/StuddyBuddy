extends layout

block content
  .detail
    h1= session.topic
    p= session.location || 'TBD'
    p= new Date(session.scheduled_time).toLocaleString('en-GB')
    p
      | Hosted by 
      a(href="/users/" + session.host_id)= session.first_name + ' ' + session.last_name
    p= 'Max: ' + (session.max_participants || 'Unlimited')

    h2 Participants
    if participants.length
      ul
        each p in participants
          li
            a(href="/users/" + p.user_id)= p.first_name + ' ' + p.last_name
            |  (#{p.status})
    else
      p No participants yet.

  a.btn(href="/sessions") Back
